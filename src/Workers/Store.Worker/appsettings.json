{
  "ConnectionStrings": {
    "Database": "Server=(localdb)\\mssqllocaldb;Database=ECommerce;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=True"
  },
  "Payments": {
    "//WebhookSecret": "Dev-only value for the in-process FakePaymentGateway (no real provider account exists) — it signs and verifies its own simulated webhooks, so this isn't a real credential. A real provider's webhook secret must come from User Secrets/environment variables, never committed — see docs/security.md.",
    "WebhookSecret": "dev-fake-gateway-webhook-secret-not-a-real-credential"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
